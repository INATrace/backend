package com.abelium.inatrace.types;

public enum ProductCompanyType {
    BUYER,
    IMPORTER,
    EXPORTER,
    OWNER,
    PRODUCER,
    ASSOCIATION,
    PROCESSOR,
    TRADER,
    // Legacy, exists for migration script
    ROASTER
}
